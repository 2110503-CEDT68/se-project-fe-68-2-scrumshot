networks:
  app-network:
    external: true

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    networks:
      - app-network
    restart: unless-stopped
